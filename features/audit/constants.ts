import { TABLE_PAGE_SIZE } from "@/lib/pagination";

/** Wie viele Einträge die kompakte Karte der Übersicht zeigt. */
export const ACTIVITY_OVERVIEW_LIMIT = 20;

/** Wie viele Einträge eine Seite beim Nachladen (Infinite Scroll) bringt. */
export const ACTIVITY_PAGE_SIZE = TABLE_PAGE_SIZE;
